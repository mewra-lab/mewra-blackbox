# Changelog

## 0.3.0 — Unreleased

- Initial VS Code extension, strict versioned configuration and digest-bound human approval.
- Isolated Chromium scenarios, exact-origin/DNS-pinned network policy, deterministic actions and assertions, cancellation and timeouts.
- Local bounded evidence, redacted action traces, input masks and screenshot suppression for secret-bearing scenarios.
- Human-reviewed visual baselines and accessible baseline/actual/diff viewer.
- PreFlight API v1 check registration and diff-scoped selection; always-visible host row action remains an upstream dependency.
- Opt-in native MCP provider for approved metadata, redacted results, bounded runs, and non-executable proposals.
- Pinned contributor skills, CI/security fixtures, VS Code host smoke tests, VSIX audits and annotated-tag release workflow.
